@model IEnumerable<RaunstrupAuth.Models.Bynavn>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Postnummer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Navn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Postnummer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Navn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Byid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Byid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Byid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
