@model IEnumerable<RaunstrupAuth.Models.Medarbejder>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>

@* laver en tabel med data fra flere tabeler og bruger en foreach til at delle alt dataen ud i viewet *@
<table class="table">
    <thead>
        <tr>
            <th>
                Medarbejder Id
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Navn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tlf)
            </th>
            <th>
                Uddannelse
            </th>
            <th>
                Færdiguddannet
            </th>
            <th>
                Adresse Id
            </th>
            <th>
                Speciale Id
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Mid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Navn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tlf)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Udd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Fudd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.A.Aid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sp.Spid)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Mid">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Mid">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Mid">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
