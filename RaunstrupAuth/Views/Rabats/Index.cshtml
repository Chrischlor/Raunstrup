@model IEnumerable<RaunstrupAuth.Models.Rabat>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Rabat Id
            </th>
            <th>
                Rabat
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.rabat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Rid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Rid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Rid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
